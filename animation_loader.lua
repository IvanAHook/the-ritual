animation_loader = {}

function animation_loader.load(file, meta)
    local animation = {}
    local quadW, quadH, imgW, imgH

    for line in love.filesystem.lines(meta) do
        for x = 0, string.len(line) do
        end
    end
end
